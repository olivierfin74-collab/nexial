import { PortfolioSurface } from '@/components/mobile-v3/PortfolioSurface'

// Real Portfolio surface: positions enriched grouped by account, with
// patrimoine totals. Mounts inside the unified v3 AppShell. No legacy
// NexialApp, no placeholder copy.
export default function PortefeuillePage() {
  return <PortfolioSurface />
}
