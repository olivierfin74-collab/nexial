import { TodaySurface } from '@/components/mobile-v3/TodaySurface'

// Lab path — kept alive for UX iteration. Mounts the exact same
// TodaySurface as the production /aujourdhui route.
export default function MobileV3PreviewPage() {
  return <TodaySurface />
}
